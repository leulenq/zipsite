<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <style>
    @page { size: 5.5in 8.5in; margin: 0; }
    body { font-family: 'Inter', sans-serif; margin: 0; padding: 0.25in; background: #f7f7f7; color: #111; }
    h1 { font-size: 28px; letter-spacing: 1px; margin: 0; text-transform: uppercase; }
    .meta { display: flex; justify-content: space-between; margin-top: 4px; font-size: 12px; letter-spacing: 1px; }
    .stats { margin-top: 16px; font-size: 12px; text-transform: uppercase; display: flex; gap: 16px; }
    .bio { margin-top: 16px; font-size: 12px; line-height: 1.6; }
    .grid { margin-top: 16px; display: grid; grid-template-columns: repeat(2, 1fr); grid-gap: 8px; }
    .grid img { width: 100%; height: 100%; object-fit: cover; }
    .hero { grid-column: span 2; height: 360px; position: relative; }
    .hero img { width: 100%; height: 100%; object-fit: cover; }
    .watermark { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%) rotate(-30deg); font-size: 48px; color: rgba(0,0,0,0.12); text-transform: uppercase; letter-spacing: 4px; }
    footer { margin-top: 20px; font-size: 10px; text-transform: uppercase; letter-spacing: 2px; display: flex; justify-content: space-between; }
  </style>
</head>
<body>
  <section>
    <h1><%= profile.first_name %> <%= profile.last_name %></h1>
    <div class="meta">
      <span><%= profile.city %></span>
      <span>ZIPSITE PORTFOLIO</span>
    </div>
    <div class="stats">
      <span>Height <strong><%= heightFeet %></strong> (<%= profile.height_cm %> cm)</span>
      <span>Measurements <strong><%= profile.measurements %></strong></span>
    </div>
    <p class="bio"><%= profile.bio_curated %></p>
    <div class="grid">
      <div class="hero">
        <% if (profile.hero_image_path) { %>
          <img src="<%= profile.hero_image_path %>" alt="Hero image">
        <% } %>
        <% if (watermark) { %>
          <div class="watermark">Refined by ZipSite</div>
        <% } %>
      </div>
      <% media.forEach((item) => { %>
        <div>
          <img src="<%= item.path %>" alt="<%= item.label %>">
        </div>
      <% }) %>
    </div>
  </section>
  <footer>
    <span>zipsite</span>
    <span><%= pdfBaseUrl %>/portfolio/<%= profile.slug %></span>
  </footer>
</body>
</html>
